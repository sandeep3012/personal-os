name: design_system
description: Personal OS Design System — tokens, typography, semantic colors, and the centralized Material 3 theme builder shared by every module.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  application: any
  decimal: ^2.3.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  platform_core: any

flutter:
