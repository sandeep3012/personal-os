// Re-export the public API from the canonical barrel.
// Import 'package:platform_storage/storage.dart' directly — this file exists
// only for tooling that expects the package name to match the library file.
library;

export 'package:platform_storage/storage.dart';
