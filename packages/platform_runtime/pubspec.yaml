name: platform_runtime
description: Personal OS Platform Runtime — event bus, service registry, module system, and lifecycle management.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.11.0 <4.0.0"

dependencies:
  platform_core: any

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
