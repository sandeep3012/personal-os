name: personal_os
description: "Personal OS — Flutter application (all platforms)."
publish_to: none
resolution: workspace
version: 0.6.0+1

environment:
  sdk: ^3.11.5

dependencies:
  application: any
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^17.3.0
  platform_core: any
  platform_runtime: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
